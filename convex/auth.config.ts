// https://eager-crayfish-85.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://eager-crayfish-85.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
